'use strict';

var apiConfigs = require('./api-configs.js');

function getOutputFiles(apiType, declarations) {
    const config = apiConfigs.apiConfigs[apiType];
    const extension = declarations ? 'd.ts' : 'ts';
    return {
        typesFile: `${config.typesFile}.${extension}`,
        queryTypesFile: `${config.queryTypesFile}.${extension}`,
    };
}

exports.getOutputFiles = getOutputFiles;
//# sourceMappingURL=get-output-files.js.map
